# TODO

## Up next

- [ ] **LIVE-VERIFY DRAG-AND-DROP (Matthew, Tk)** — `pip install tkinterdnd2`, launch the Tk UI, drag one file (then several at once) onto the editor: token(s) appear in your block, peers receive them, an image drop also shows in the own-preview strip. Confirm the Attach button still works, and that launching *without* tkinterdnd2 installed still starts cleanly (drops just ignored). Anything odd → `~/.netnotepad/log.txt`.
- [ ] **Tk: image preview inside the OWN editable block** — the strip below the pane covers v1; true inline-at-token rendering in the own pane needs a solution for embedded objects corrupting the char-offset mirror (the own Text widget's content IS the document). Peers-pane inline embeds landed 2026-06-10.
- [ ] **Term renderer: image previews?** — terminals can't show bitmaps portably; could shell out to the OS viewer on the save-as path, or sixel/kitty protocols for the brave. Low priority.

## Later

- [ ] Multi-block per peer (named panes within one peer, e.g. "scratch", "URLs").
- [ ] Periodic snapshot reconciliation - detect dropped `Delta` ops via `seq` gaps and resync.
- [ ] Cross-subnet / VPN-friendly discovery (multicast doesn't traverse subnets).
- [ ] CI matrix that builds Windows / macOS / Linux binaries via PyInstaller on GitHub Actions runners.
- [ ] Optional: encrypt the mesh channel with a shared LAN passphrase, for users who don't trust their LAN.

## Verified

- [x] **2026-06-30** - Drag-and-drop file attaching (Tk). Drop file(s) onto the editor to attach — same path as the Attach button (engine.attach_file caches + broadcasts + returns a token). Optional `tkinterdnd2` dep with graceful fallback (no dep → drops ignored, button still works). New display-free `netnotepad/renderer/dnd.py::parse_drop_paths` handles the tkdnd brace-wrapped Tcl-list payload; thin Tk wiring registers the own text widget as a DND_FILES target via `TkinterDnD._require(root)` + `_on_drop`. 11 new tests (`tests/test_dnd.py`); local suite 136 green. Tk wiring + tkdnd need a display — live verification is Matthew's (see Up next). Process note: the tail edit hit the mount-drift truncation again and *py_compile passed on the truncated file* (cut landed on a valid bare-name statement) — caught via bytecode-constant marker scan, fixed with a guarded rebuild script. See WORKLOG 2026-06-30.
- [x] **2026-06-30** - Field run passed (Matthew, live LAN). Everything since `7888ba9` confirmed on real boxes: term renderer basics + attach path, Tk image previews render within ~2s, File menu open/save/peer-export, legacy cp1252 files open without mojibake. Image thumbnails confirmed rendering. Commit `57338e9` (File open / Save As / peer-block export) landed and pushed — the stuck `.git\index.lock` cleared from Windows.
- [x] **2026-06-12** - File → Open / Save As / peer-block export, both renderers. New display-free `netnotepad/engine/fileio.py`: `read_text_for_open` (5 MB cap + NUL-byte binary sniff — the opened text becomes a Snapshot broadcast to every peer, so garbage is rejected at the door; utf-8-sig first then cp1252 fallback for legacy Notepad files; CRLF/CR normalized to LF since stray `\r` would corrupt grapheme-position arithmetic between peers), `export_text` (atomic temp+rename, mirrors `Document.save`), `block_choices` (own first, then snapshot-received peers — tombstoned included, exporting a departed peer's last-known text is the rescue case), `safe_filename` (Windows-reserved chars). Tk: File menubar — Open (Ctrl+O, pre-empts Tk's default openline binding via `return "break"`), Save As (Ctrl+Shift+S), Save Block As submenu rebuilt from live peer state via `postcommand`. Open is just delete+insert on the Text widget — the existing `<<Modified>>` mirror does the Snapshot broadcast and autosave scheduling. Term: Ctrl-O / Ctrl-E through the existing dialog-bar pattern; Ctrl-E skips the numbered pick when it's just us; directory destinations reuse `_resolve_dest` with a `safe_filename(label).txt` default. Open goes through `set_local_text` (Snapshot path) rather than a giant Delta. 22 new tests (15 fileio + 7 term integration); local suite 123 green. Tk wiring needs a display — live verification is Matthew's, as ever.
- [x] **2026-06-10** - Inline image previews (Tk) + term-renderer line wrap. Term: own block now `wrap_lines=True` (Matthew's call; Up/Down move by logical line). Tk: image attachments render as thumbnails — peers pane embeds them at the END of each peer's section via `image_create` (embedded images count as one index position each in Tk, so the surgical-update `content_len` includes them and the attachment-tag offsets over header+body stay valid); own images show in a strip below the editable pane (embedding objects in the own Text widget would corrupt the char-offset mirror that IS the document). New display-free module `netnotepad/renderer/preview.py`: `is_previewable_image` (png/gif/ppm native; +jpeg/webp/bmp/tiff/ico when Pillow is installed — optional), `subsample_factor` (ceil integer downscale to 280px), `cached_preview_shas` (ordered, deduped, cached-only). `_peer_fingerprint` now takes the store so a finished prefetch changes the fingerprint; a 2s `root.after` tick notices it (prefetch completion fires no peer event). PhotoImage refs held in a sha-keyed cache (Tk keeps no Python ref — GC would blank the previews). 5 new tests (`tests/test_preview.py`); local suite 101 green. Tk wiring itself needs a display — live LAN verification is Matthew's, as ever.
- [x] **2026-06-10** - Terminal renderer attach/save-as UI. Dialog bar (single line at the bottom of the HSplit, `ConditionalContainer`) whose text lives in the shared `ui` dict — no prompt_toolkit `Buffer`, consistent with the engine-owns-the-model rule; `Condition` filters swap every key binding between document mode and dialog mode (Ctrl-Q/Ctrl-C stay global). **Ctrl-A**: path prompt -> `engine.attach_file` -> token inserted via `engine.insert` (Delta wire path; offer broadcast by `attach_bytes`); error cases (missing file, directory, permission, >50MB) surface as transients. **Ctrl-D**: token under cursor (`_token_at_cursor`, grapheme-col -> str-index conversion, `s <= idx <= e`) or numbered pick over `_visible_attachments` (own block first, then peers, sha-deduped); destination prompt accepts a directory (keeps original filename, `_resolve_dest`); cached blobs save synchronously, uncached peer blobs fetch on a daemon thread via `engine.ensure_attachment`. Empty Enter / Escape / Ctrl-G cancel. 9 new tests (34 term total); local suite 96 green (16 document, 13 logging, 25 attachments, 8 network-unit, 34 term).
- [x] **2026-06-10** - Terminal renderer follow-ups, part 1. Remembered (goal) column on Up/Down: `_move_up`/`_move_down` take an optional `goal` (back-compatible); the binding layer keeps `ui["goal_col"]` for a vertical run and forgets it on any horizontal move or edit (`mark_dirty`). Peers-pane paging: PageUp/PageDown scroll the (unfocused) peers pane via `_page_scroll` clamping; the pane's FormattedTextControl pins a synthetic cursor at the scroll line because prompt_toolkit clamps `vertical_scroll` to keep the content cursor visible (without it the pane snaps back to top each render). Also: README install lines + renderer/layout/feature sections refreshed (attachments + term renderer no longer listed as unbuilt), `start.bat` now does the pip install the README promised and its `%%~dp0` escaping bug is fixed (`%~dp0`). 7 new tests (goal-column pure + integration, `_page_scroll` pure, paging integration via `app._netnotepad_peers_window`). Local suite 87 green (16 document, 13 logging, 25 attachments, 8 network-unit, 25 term); 13 real-mDNS tests stay for live LAN verification.
- [x] **2026-06-08** - Terminal renderer (`prompt_toolkit`). `netnotepad/renderer/term_renderer.py` + `--renderer term` wired in `__main__.py`. Drives editing through the engine's Delta path (`engine.insert` / `delete_backward` / `delete_forward` / `move_cursor`) — the wire path the Tk renderer (Snapshot via `set_local_text`) never exercises. Own block is a `FormattedTextControl` whose text is `engine.document.text` and whose cursor is `engine.document.cursor` via `get_cursor_position`; a `KeyBindings` set maps keys to engine calls (printable -> insert; Enter -> newline; Tab -> 4 spaces; Backspace/Delete; arrows/Home/End -> `move_cursor` via grapheme-aware target helpers; Ctrl-S save; Ctrl-Q/Ctrl-C quit). Full-screen layout: status bar / editable own block / read-only peers pane (same body-state logic as Tk: text / (empty) / (no content received yet); dimmed if tombstoned). Networking on a daemon thread; peer callbacks only `app.invalidate()`; saves run on the event-loop thread so they never race document edits. Attachment tokens render highlighted in both panes; attach/save-as UI deferred (see Up next). 18 headless tests (`create_pipe_input()` + `DummyOutput`) covering pure cursor/format helpers and key->Delta integration (asserting both final text and the emitted Delta sequence). Local-testable suite 80 green (16 document, 13 logging, 25 attachments, 8 network-unit, 18 term); the 13 real-mDNS e2e/discovery tests stall in the Cowork Linux sandbox (no reliable multicast) and stay for live LAN verification. Dep added: `prompt_toolkit>=3.0` (pyproject + `build_exe.bat --collect-all prompt_toolkit`).
- [x] **2026-05-19** - Attachments end-to-end. `engine/attachments.py` is the real module now: `AttachmentStore` (content-addressed cache at `~/.netnotepad/attachments/<sha>`, atomic temp+rename writes), `AttachmentServer` (`ThreadingHTTPServer` with `GET /blob/<sha>`, mirrors Mesh's port-fallback pattern), `fetch_blob` (urllib client that verifies sha on receipt). Engine wiring: server boots in `start_networking` before discovery so the TXT record carries the actually-bound port; `_on_remote_message` captures attach_port from Hello and records `AttachmentOffer` on `Peer.known_attachments`, kicking a background prefetch; public `attach_bytes`/`attach_file` hash + cache + broadcast offer; public `ensure_attachment(peer, sha)` is the sync retry path. Tk minimal v1: "Attach file..." button, green-underlined token tag in both panes, right-click "Save attachment as..." that fetches on demand for peer tokens. `MAX_ATTACHMENT_BYTES = 50 MB`. Image-rendering of inline tokens stays Later. 25 new tests in `tests/test_attachments.py` (token helpers, store, server, fetch client, engine integration). Full suite: 75/75 green.
- [x] **2026-05-13** - Document model unit tests pass (16/16).
- [x] **2026-05-13** - Discovery end-to-end smoke test passes - two Discovery instances find each other via mDNS.
- [x] **2026-05-13** - Network end-to-end tests pass (3/3) - two engines mutually discover, bidirectional sync works, late-joining peer receives initial snapshot.
- [x] **2026-05-13** - Full suite: 26/26 green.
- [x] **2026-05-14** - Diagnostic logging wired in. `error_handler.py` vendored into the package; new `netnotepad/log.py` exposes `log_info` / `log_exception` / `crash_to_file` / `set_log_dir`. Every silent except in the mesh, discovery, document, and Tk layers now routes through `log_exception(e, context=...)` into `~/.netnotepad/log.txt`. `__main__.py` wraps everything in an outer try/except that writes a `for_claude()` heavy report to `~/.netnotepad/last_crash.txt` on any unhandled crash. 9 new tests added in `tests/test_logging.py`. Full suite: 35/35 green.
- [x] **2026-05-14** - Tombstone grace period (5s) prevents UI flicker on transient network blips. TCP drop schedules a delayed tombstone instead of firing immediately; any inbound traffic during the grace window cancels it. Trade-off: a real disconnect takes ~5s longer to show as offline. Tests `test_tombstone_grace_suppresses_flicker_on_quick_reconnect` and `test_tombstone_fires_after_grace_when_no_reconnect` cover both paths. Full suite: 37/37 green.
- [x] **2026-05-14** - Windows packaging via PyInstaller. `run.py` entrypoint shim + `build_exe.bat` produces `dist\netnotepad.exe` as a single self-contained binary. Documented PATH placement (personal `%USERPROFILE%\bin`, not `C:\Windows`).
- [x] **2026-05-16** - Log rotation. `log.txt` capped at 1 MB (`LOG_MAX_BYTES`); on overflow it rolls to `log.1.txt` (single backup, prior backup overwritten). Implemented as `_maybe_rotate()` called under `_LOCK` at the head of `_append`. 4 new tests in `tests/test_logging.py` cover: rotation triggers at threshold, prior backup overwritten on second rotation, no rotation below threshold, no crash when log.txt doesn't exist yet. Full suite: 41/41 green.
- [x] **2026-05-16** - Apply remote Deltas to `peer.block_text`. Engine now applies incoming `Delta` messages via `_apply_delta_to_peer` (grapheme-aware, reuses `document._graphemes`, bounds-clamped defensively). Drops Deltas that arrive before the initial Snapshot for that peer (no authoritative base to build on); the 30s periodic Snapshot rebroadcast is the eventual-consistency fallback. 9 new tests in `tests/test_network.py` cover: pure insert / pure delete / replace / out-of-bounds pos clamp / out-of-bounds remove clamp / grapheme-cluster correctness (family-of-four emoji) / drop-before-snapshot / apply-after-snapshot / end-to-end propagation via `engine.insert` (exercises the Delta wire path that the future terminal renderer will use). Full suite: 50/50 green.
- [x] **2026-05-13** - First two-machine test (Matthew's LAN): instances on ID10TError-Laptop1 and DESKTOP-NM6GRPH discovered each other and exchanged initial snapshots, but live edits didn't propagate. Diagnosed as mDNS-flicker tombstones plus no auto-reconnect post-TCP-drop. Hardening pass applied:
    - Mesh.start() falls back to a kernel-assigned port if the requested one is taken (fixes "doesn't like being run more than once on the same PC").
    - Discovery-driven tombstone is suppressed when the TCP connection is still alive (mDNS flicker no longer marks live peers offline).
    - On TCP disconnect, the engine schedules a reconnect attempt after 2s using the last-known DiscoveredPeer; lex-order rules inside maybe_connect make this a no-op on the "should accept" side.
    - Heartbeat interval tightened to 5s; a watchdog thread closes any connection that hasn't received traffic for 25s, which then triggers the auto-reconnect path.
    - Periodic snapshot rebroadcast every 30s defensively catches any silently-dropped message.
    - All 26 existing tests still pass after the hardening pass.
